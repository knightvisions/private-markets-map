import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Private Markets Infrastructure Map",
  description:
    "Interactive market map of private markets infrastructure: cap tables, fund admin, SPVs, secondaries, LP portals, forward contracts, broker-dealers, data & compliance.",
  openGraph: {
    title: "Private Markets Infrastructure Map",
    description:
      "Explore 100+ companies across the private markets software stack on a 2D grid by category and customer type.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
